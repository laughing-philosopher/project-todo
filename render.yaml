# render.yaml
services:
  - type: web
    name: project-completion
    runtime: python
    buildCommand: ""
    startCommand: python app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.9
